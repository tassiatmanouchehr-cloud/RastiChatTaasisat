# Everything the launcher generates at run time is machine-specific
# (LAN IP, PIDs, timestamps, local ports actually chosen) and must never be
# committed. Nothing under runtime/ is source — it is always regenerated by
# `start`/`status`/`urls`.
runtime/
*.log
